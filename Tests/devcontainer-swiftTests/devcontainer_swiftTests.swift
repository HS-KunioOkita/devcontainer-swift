import XCTest
@testable import devcontainer_swift

final class devcontainer_swiftTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(devcontainer_swift().text, "Hello, World!")
    }
}
